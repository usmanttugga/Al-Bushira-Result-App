const data = { students:[], classes:[], subjects:[], results:[], users:[ {username:'admin',password:'password',role:'admin'}, {username:'teacher',password:'password',role:'user'} ] }; fetch('https://jsonblob.com/api/jsonBlob', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify(data) }).then(r => console.log('JSONBlob Location:', r.headers.get('location'))).catch(console.error);
